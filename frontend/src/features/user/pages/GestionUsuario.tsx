import { type ReactElement } from "react";
import GestionUsuarios from "../components/GestionUsuario";
// import bg from "../assets/bg-login.jpg";

export default function GestionUsuariosPage(): ReactElement {
  return (
    
      // <div className="min-h-screen flex items-center justify-center bg-cover bg-center relative" style={{ backgroundImage: `url(${bg})` }}>
      // <div className="absolute inset-0 bg-black/40 backdrop-blur-sm"></div>
      // <div className="relative z-10 w-full flex items-center justify-center p-8">
        <GestionUsuarios />
    //   </div>

    // </div>
    
  );
}
